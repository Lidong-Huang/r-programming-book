{
  "hash": "e25e82d82345997c29a2da52bdafd113",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"R中的字符串处理\"\n---\n\n## 本章学习目标\n\n通过本章学习，你将能够：\n\n- 掌握字符串长度、截取、大小写等基本操作\n- 熟练使用 `paste()` 和 `paste0()` 拼接字符串\n- 使用 `strsplit()` 拆分和 `trimws()` 清洗字符串\n- 使用 `sprintf()` 进行格式化输出\n- 掌握 `grep()`、`sub()`、`gsub()` 进行模式匹配与替换\n- 使用 `regexpr()` + `regmatches()` 提取匹配内容\n- 能够独立完成字符串数据清洗任务\n\n---\n\n## 字符串长度与字符统计\n\n你已经见过 `nchar()`：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnchar(\"hello\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 5\n```\n\n\n:::\n:::\n\n\n👉 `nchar()` 返回字符数；`type = \"bytes\"` 返回字节数（中文字符通常占 2-3 个字节）。\n\n---\n\n`nchar()` 和 `length()` 是两回事：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nx <- c(\"a\", \"abc\", \"abcde\")\nlength(x)     # 3 —— 向量里有几个元素\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 3\n```\n\n\n:::\n\n```{.r .cell-code}\nnchar(x)      # 1 3 5 —— 每个元素有几个字符\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1 3 5\n```\n\n\n:::\n:::\n\n\n👉 一个问\"向量多长\"，一个问\"每个字符串多长\"。不要搞混。\n\n---\n\n## 字符串截取与替换\n\n`substr(x, start, stop)` 按位置截取子串：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsubstr(\"hello\", 2, 4)           # \"ell\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"ell\"\n```\n\n\n:::\n:::\n\n\n---\n\n`substr()` 还能用于**原地修改**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nx <- \"hello\"\nsubstr(x, 1, 1) <- \"H\"\nx                              # \"Hello\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"Hello\"\n```\n\n\n:::\n:::\n\n\n---\n\n## 大小写转换\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntoupper(\"hello\")               # \"HELLO\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"HELLO\"\n```\n\n\n:::\n\n```{.r .cell-code}\ntolower(\"HELLO\")               # \"hello\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"hello\"\n```\n\n\n:::\n\n```{.r .cell-code}\ncasefold(\"HELLO\")              # \"hello\"（默认 upper = FALSE）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"hello\"\n```\n\n\n:::\n\n```{.r .cell-code}\ncasefold(\"hello\", upper = TRUE) # \"HELLO\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"HELLO\"\n```\n\n\n:::\n:::\n\n\n`chartr()` 可以按字符一一对应地翻译：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nchartr(\"a-z\", \"A-Z\", \"hello\")   # \"HELLO\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"HELLO\"\n```\n\n\n:::\n\n```{.r .cell-code}\nchartr(\"aeiou\", \"AEIOU\", \"hello\")  # \"hEllO\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"hEllO\"\n```\n\n\n:::\n:::\n\n\n---\n\n## 字符串拼接\n\n`paste()` 和 `paste0()` 是 R 中最常用的拼接函数：\n\n\n::: {.cell}\n\n```{.r .cell-code}\npaste(\"A\", \"B\", \"C\")            # \"A B C\"（默认空格分隔）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"A B C\"\n```\n\n\n:::\n\n```{.r .cell-code}\npaste(\"A\", \"B\", sep = \"-\")      # \"A-B\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"A-B\"\n```\n\n\n:::\n\n```{.r .cell-code}\npaste0(\"A\", \"B\", \"C\")           # \"ABC\"（等价于 paste(..., sep = \"\")）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"ABC\"\n```\n\n\n:::\n:::\n\n\n👉 记住：`paste0()` = `paste(sep = \"\")`，不带分隔符。\n\n---\n\n向量化拼接——不同长度的向量会自动循环：\n\n\n::: {.cell}\n\n```{.r .cell-code}\npaste(\"sample\", 1:3, sep = \"_\")   # \"sample_1\" \"sample_2\" \"sample_3\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"sample_1\" \"sample_2\" \"sample_3\"\n```\n\n\n:::\n:::\n\n\n`collapse` 参数把向量**折叠成单个字符串**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\npaste(c(\"A\", \"B\", \"C\"), collapse = \", \")  # \"A, B, C\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"A, B, C\"\n```\n\n\n:::\n\n```{.r .cell-code}\npaste(letters[1:5], collapse = \"-\")       # \"a-b-c-d-e\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"a-b-c-d-e\"\n```\n\n\n:::\n:::\n\n\n👉 `sep` 是多个参数之间的分隔符，`collapse` 是把向量元素合并成一个。\n\n---\n\n## 字符串拆分\n\n`strsplit()` 按分隔符拆分，**返回列表**：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nstrsplit(\"a,b,c\", split = \",\")            # 返回列表\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[[1]]\n[1] \"a\" \"b\" \"c\"\n```\n\n\n:::\n\n```{.r .cell-code}\nstrsplit(\"a,b,c\", split = \",\")[[1]]       # c(\"a\", \"b\", \"c\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"a\" \"b\" \"c\"\n```\n\n\n:::\n:::\n\n\n👉 为什么返回列表？因为拆分后每段长度可能不同，只有列表能容纳。\n\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 拆分为单个字符\nstrsplit(\"abcdef\", split = \"\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[[1]]\n[1] \"a\" \"b\" \"c\" \"d\" \"e\" \"f\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# 向量化拆分\nstrsplit(c(\"a,b\", \"c,d,e\"), split = \",\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[[1]]\n[1] \"a\" \"b\"\n\n[[2]]\n[1] \"c\" \"d\" \"e\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# unlist 拉平\nunlist(strsplit(c(\"a,b\", \"c,d,e\"), split = \",\"))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"a\" \"b\" \"c\" \"d\" \"e\"\n```\n\n\n:::\n:::\n\n\n---\n\n## 去除空白\n\n`trimws()` 去除首尾空白：\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntrimws(\"  hello  \")                  # \"hello\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"hello\"\n```\n\n\n:::\n\n```{.r .cell-code}\ntrimws(\"  hello  \", which = \"left\")  # \"hello  \"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"hello  \"\n```\n\n\n:::\n\n```{.r .cell-code}\ntrimws(\"  hello  \", which = \"right\") # \"  hello\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"  hello\"\n```\n\n\n:::\n:::\n\n\n清洗数据框中的字符列：\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndf <- data.frame(name = c(\" 张三 \", \" 李四  \"), score = 1:2)\ndf$name <- trimws(df$name)\ndf\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  name score\n1 张三     1\n2 李四     2\n```\n\n\n:::\n:::\n\n\n---\n\n## 格式化输出\n\n`sprintf()` 用格式占位符生成指定格式的字符串：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsprintf(\"编号%03d\", 7)            # \"编号007\"（零填充到3位）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"编号007\"\n```\n\n\n:::\n\n```{.r .cell-code}\nsprintf(\"%.2f\", 3.14159)          # \"3.14\"（两位小数）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"3.14\"\n```\n\n\n:::\n\n```{.r .cell-code}\nsprintf(\"%s：%d\", \"样本\", 1:3)    # \"样本：1\" \"样本：2\" \"样本：3\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"样本：1\" \"样本：2\" \"样本：3\"\n```\n\n\n:::\n\n```{.r .cell-code}\nsprintf(\"%10s\", \"abc\")            # \"       abc\"（右对齐，宽度10）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"       abc\"\n```\n\n\n:::\n\n```{.r .cell-code}\nsprintf(\"%-10s\", \"abc\")           # \"abc       \"（左对齐，宽度10）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"abc       \"\n```\n\n\n:::\n\n```{.r .cell-code}\nsprintf(\"%05d\", 42)               # \"00042\"（整数零填充）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"00042\"\n```\n\n\n:::\n:::\n\n\n常用格式符：\n\n| 格式符 | 含义 |\n|--------|------|\n| `%s` | 字符串 |\n| `%d` | 整数 |\n| `%f` | 浮点数 |\n| `%e` | 科学计数法 |\n\n```r\n\nfor (i in 1:10) {\n  \n  filename <- sprintf(\"Fig_%02d.png\", i)\n  \n  png(filename, width = 800, height = 600)\n  \n  x <- 1:20\n  y <- rnorm(20, mean = i * 2, sd = 3)\n  \n  plot(x, y, type = \"b\", pch = 16, col = i + 1, lwd = 2,\n       main = paste0(\"Figure_\", i),\n       xlab = \"x\", ylab = \"y\")\n  \n  dev.off()\n}\n\n```\n\n---\n\n## 模式匹配：查找\n\n`grep()` 返回匹配元素的索引，`grepl()` 返回逻辑向量：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nx <- c(\"apple\", \"banana\", \"grape\", \"pear\", \"apricot\")\n\ngrep(\"^a\", x)                     # 1 5（索引）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1 5\n```\n\n\n:::\n\n```{.r .cell-code}\ngrep(\"^a\", x, value = TRUE)       # \"apple\" \"apricot\"（值）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"apple\"   \"apricot\"\n```\n\n\n:::\n\n```{.r .cell-code}\ngrepl(\"^a\", x)                    # TRUE FALSE FALSE FALSE TRUE\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1]  TRUE FALSE FALSE FALSE  TRUE\n```\n\n\n:::\n:::\n\n\n👉 `grepl()` 返回逻辑向量，可以直接用于 `[ ]` 筛选或 `subset()`。\n\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 逻辑向量筛选\nx[grepl(\"a\", x)]                  # 所有含 a 的元素\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"apple\"   \"banana\"  \"grape\"   \"pear\"    \"apricot\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# 忽略大小写\ngrep(\"A\", c(\"apple\", \"Apple\"), ignore.case = TRUE)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1 2\n```\n\n\n:::\n\n```{.r .cell-code}\n# 反向匹配\ngrep(\"a\", x, invert = TRUE)       # pear（不含 a 的元素）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\ninteger(0)\n```\n\n\n:::\n:::\n\n\n---\n\n## 模式匹配：替换\n\n`sub()` 替换首次匹配，`gsub()` 替换全部匹配：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nsub(\"a\", \"X\", \"banana\")           # \"bXnana\"（仅第一个）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"bXnana\"\n```\n\n\n:::\n\n```{.r .cell-code}\ngsub(\"a\", \"X\", \"banana\")          # \"bXnXnX\"（全部）\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"bXnXnX\"\n```\n\n\n:::\n:::\n\n\n👉 工作中**绝大多数情况用 `gsub()`**，因为通常要把所有匹配都替换掉。\n\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 删除多余空格\ngsub(\"\\\\s+\", \" \", \"a  b   c\")    # \"a b c\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"a b c\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# 向量化替换\nfruits <- c(\"apple\", \"pear\")\ngsub(\"p\", \"P\", fruits)            # \"aPPle\" \"Pear\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"aPPle\" \"Pear\" \n```\n\n\n:::\n\n```{.r .cell-code}\n# 删除所有数字\ngsub(\"\\\\d\", \"\", \"abc123def456\")   # \"abcdef\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"abcdef\"\n```\n\n\n:::\n:::\n\n\n---\n\n## 模式匹配：提取\n\n`regexpr()` / `gregexpr()` 返回匹配位置，配合 `regmatches()` 提取匹配文本：\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 提取首次匹配\nx <- \"ID编号007\"\nm <- regexpr(\"\\\\d+\", x)\nregmatches(x, m)                  # \"007\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"007\"\n```\n\n\n:::\n:::\n\n\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 提取全部匹配\nx <- \"a1b22c333\"\nm <- gregexpr(\"\\\\d+\", x)\nregmatches(x, m)                  # list(\"1\", \"22\", \"333\")\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[[1]]\n[1] \"1\"   \"22\"  \"333\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# 提取邮箱域名（需要 perl = TRUE 支持后顾断言）\nemail <- \"user@example.com\"\nm <- regexpr(\"(?<=@)\\\\S+\", email, perl = TRUE)\nregmatches(email, m)              # \"example.com\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"example.com\"\n```\n\n\n:::\n:::\n\n\n---\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# 批量提取\nemails <- c(\"a@gmail.com\", \"b@163.com\", \"c@qq.com\")\nm <- gregexpr(\"(?<=@)\\\\S+\", emails, perl = TRUE)\nunlist(regmatches(emails, m))     # \"gmail.com\" \"163.com\" \"qq.com\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"gmail.com\" \"163.com\"   \"qq.com\"   \n```\n\n\n:::\n:::\n\n\n👉 `perl = TRUE` 启用 PCRE 引擎，支持后顾断言 `(?<=...)` 等高级特性。\n\n---\n\n## 正则表达式速查\n\nR 默认使用 TRE 正则引擎，设置 `perl = TRUE` 可使用 PCRE 引擎。\n\n| 模式 | 含义 | 示例 |\n|------|------|------|\n| `.` | 任意单个字符 | `a.b` 匹配 \"aab\" |\n| `^` | 行首 | `^a` 以 a 开头 |\n| `$` | 行尾 | `a$` 以 a 结尾 |\n| `\\\\d` | 数字 `[0-9]` | `\\\\d+` 提取数字 |\n| `\\\\w` | 单词字符 | `\\\\w+` 提取单词 |\n| `\\\\s` | 空白字符 | `\\\\s+` 匹配连续空格 |\n| `[abc]` | 字符类 | a、b 或 c |\n| `[^abc]` | 否定字符类 | 非 a、b、c |\n| `*` | 零次或多次 | `a*` |\n| `+` | 一次或多次 | `a+` |\n| `?` | 零次或一次 | `a?` |\n| `{n}` | 恰好 n 次 | `\\\\d{4}` 4位数字 |\n| `{n,m}` | n 到 m 次 | `\\\\d{2,4}` |\n| `()` | 捕获分组 | `(\\\\d+)-(\\\\d+)` |\n| `|` | 或 | `apple\\|banana` |\n\n\n::: {.cell}\n\n```{.r .cell-code}\ngrep(\"^\\\\d{3}-\\\\d{4}\", c(\"010-1234\", \"01-1234\"))  # 匹配电话区号\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] 1\n```\n\n\n:::\n\n```{.r .cell-code}\ngsub(\"[[:punct:]]\", \"\", \"Hello, World!\")            # 去掉所有标点\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"Hello World\"\n```\n\n\n:::\n\n```{.r .cell-code}\ngsub(\"[^0-9]\", \"\", \"电话：138-1234-5678\")           # 只保留数字\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"13812345678\"\n```\n\n\n:::\n:::\n\n\n---\n\n## 综合应用示例\n\n批量处理文件名——提取日期并转格式：\n\n\n::: {.cell}\n\n```{.r .cell-code}\nfiles <- c(\"report_20240115.csv\", \"data_20231201.pdf\",\n           \"summary_20240320.xlsx\")\n\n# 提取 8 位日期\nm <- regexpr(\"\\\\d{8}\", files)\ndates <- regmatches(files, m)\ndates  # \"20240115\" \"20231201\" \"20240320\"\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"20240115\" \"20231201\" \"20240320\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# 转为标准格式\ngsub(\"(\\\\d{4})(\\\\d{2})(\\\\d{2})\", \"\\\\1-\\\\2-\\\\3\", dates)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] \"2024-01-15\" \"2023-12-01\" \"2024-03-20\"\n```\n\n\n:::\n\n```{.r .cell-code}\n# \"2024-01-15\" \"2023-12-01\" \"2024-03-20\"\n```\n:::\n\n\n---\n\n数据框清洗——统一清理字符列：\n\n\n::: {.cell}\n\n```{.r .cell-code}\ndf <- data.frame(\n  name  = c(\" 张三 \", \"李  四\", \"王五\"),\n  phone = c(\"138-1234-5678\", \"(010)82345678\", \"13912345678\"),\n  stringsAsFactors = FALSE\n)\n\ndf$name <- trimws(df$name)                         # 去首尾空白\ndf$name <- gsub(\"\\\\s+\", \"\", df$name)               # 去内部空格\ndf$phone <- gsub(\"[^0-9]\", \"\", df$phone)           # 只保留数字\ndf\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n  name       phone\n1 张三 13812345678\n2 李四 01082345678\n3 王五 13912345678\n```\n\n\n:::\n:::\n\n\n---\n\n文本分词与词频统计：\n\n\n::: {.cell}\n\n```{.r .cell-code}\ntext <- c(\"R is a language for statistical computing\",\n          \"Python is a general purpose language\")\n\nwords <- unlist(strsplit(tolower(text), \" \"))\nfreq <- sort(table(words), decreasing = TRUE)\nhead(freq, 8)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\nwords\n        a        is  language computing       for   general   purpose    python \n        2         2         2         1         1         1         1         1 \n```\n\n\n:::\n:::\n\n\n---\n\n## 本章总结\n\n- **长度**：`nchar()` 字符数，`length()` 元素数\n- **截取**：`substr()` / `substring()` 按位置提取，`substr()` 可赋值修改\n- **大小写**：`toupper()`、`tolower()`、`casefold()`；`chartr()` 字符翻译\n- **拼接**：`paste()` / `paste0()`，`sep` 分隔参数，`collapse` 折叠向量\n- **拆分**：`strsplit()`，返回列表，用 `[[ ]]` 或 `unlist()` 提取\n- **空白**：`trimws()` 去首尾空白\n- **格式化**：`sprintf()` 用 `%s`、`%d`、`%f` 生成格式化字符串\n- **查找**：`grep()` 返回索引，`grepl()` 返回逻辑值\n- **替换**：`sub()` 换首个，`gsub()` 换全部\n- **提取**：`regexpr()` / `gregexpr()` + `regmatches()` 提取匹配内容\n\n👉 **字符串处理是数据清洗的基本功。** 打开 RStudio，把每个函数的例子都动手跑一遍。\n",
    "supporting": [
      "lecture12_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}